const initialState = {
    loggedIn: false,
    signUp: false,
    loggingIn: false,
    signingUp: false
}

const login = (state = initialState, action) => {
    switch(action.type) {
        case 
    }
}